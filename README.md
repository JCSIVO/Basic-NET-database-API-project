# Basic-NET-database-API-project
building a simple REST API, that will be able to perform a CRUD on a complex object type. That is to say, insertion, retrieval, modification and deletion of information units.
